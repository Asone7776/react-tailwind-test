const HomePage = () => {
    // const {data} = useSWR('https://dummyjson.com/recipes', (url) => instance.get(url, {
    //     params: {
    //         status: 1
    //     },
    // }));
    return (
        <>
            Lorem
        </>
    )
}

export default HomePage
